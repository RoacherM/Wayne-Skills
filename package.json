{
  "name": "wayne-skills",
  "version": "0.1.0",
  "description": "Wayne's agent skills: okr (goal and task tracking with a terminal dashboard) and terminal-diagrams (mmd2txt: Mermaid to box art for agents whose user reads in a terminal).",
  "repository": "github:RoacherM/Wayne-Skills",
  "homepage": "https://github.com/RoacherM/Wayne-Skills#readme",
  "type": "module",
  "bin": {
    "okr": "./bin/okr.js"
  },
  "files": [
    "bin",
    "skills/okr",
    "skills/terminal-diagrams"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "bundle": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.2",
    "grok-mermaid": "^0.2.2",
    "typescript": "^5.6.0",
    "yaml": "^2.6.0"
  }
}
