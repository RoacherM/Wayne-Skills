{
  "name": "wayne-skills",
  "version": "0.1.0",
  "description": "Wayne's agent skills, plus the okr CLI: goal and task tracking with a terminal dashboard. Events in, views out.",
  "repository": "github:RoacherM/Wayne-Skills",
  "homepage": "https://github.com/RoacherM/Wayne-Skills#readme",
  "type": "module",
  "bin": {
    "okr": "./bin/okr.js"
  },
  "files": [
    "bin",
    "skills/okr"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "demo": "node bin/okr.js tui --demo",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.0",
    "yaml": "^2.6.0"
  }
}
